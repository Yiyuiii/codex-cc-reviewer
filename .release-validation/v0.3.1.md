# Local Codex Release Validation: v0.3.1

RC: v0.3.1-rc.0
Doctor: pass
Local-Codex-Smoke: pass
cc_review: pass

## Environment

- Date: 2026-05-14
- Maintainer: yiyuiii
- Repository: `Yiyuiii/codex-cc-reviewer`
- Branch: `next`
- RC commit: `116c3c3 chore: prepare v0.3.1-rc.0`
- RC tag: `v0.3.1-rc.0`
- npm dist-tag validated: `next -> 0.3.1-rc.0`
- GitHub prerelease: `https://github.com/Yiyuiii/codex-cc-reviewer/releases/tag/v0.3.1-rc.0`

## Commands

```powershell
npm run verify:release
npm view codex-cc-reviewer@0.3.1-rc.0 version
npm view codex-cc-reviewer@next version
npx --prefer-online -y codex-cc-reviewer@next --version
npx --prefer-online -y codex-cc-reviewer@next install --package-spec codex-cc-reviewer@next
npx --prefer-online -y codex-cc-reviewer@next doctor
```

After restarting Codex, the restarted session invoked the MCP `cc_review` tool with:

```text
task: review_diff
model: opus
effort: max
permissionMode: bypassPermissions
tools: default
cwd: E:\Codes\codex-cc-reviewer
redactSecrets: true
includeGitStatus: true
includeGitDiff: true
includeUntrackedContent: true
context: Local Codex post-restart smoke test for codex-cc-reviewer v0.3.1-rc.0. Verify tracked diff routing and selected untracked evidence after restart.
```

## Results

- `npm run verify:release`: passed before rc publication. It ran typecheck, full Vitest suite, build, `npm pack --dry-run --json`, and CLI `--version` / `--help`.
- Vitest before rc publication: 18 files, 120 tests passed.
- Build: passed.
- Package dry run: produced `codex-cc-reviewer-0.3.1-rc.0.tgz` with 23 package entries.
- CLI version before stable bump: `0.3.1-rc.0`.
- GitHub Release workflow for `v0.3.1-rc.0`: passed publish and GitHub Release jobs. The publish job used npm Trusted Publishing provenance and `--tag next`.
- npm registry: `codex-cc-reviewer@0.3.1-rc.0` resolved to `0.3.1-rc.0`, and `codex-cc-reviewer@next` resolved to `0.3.1-rc.0`.
- `npx --prefer-online -y codex-cc-reviewer@next --version`: `0.3.1-rc.0`.
- `doctor`: exited successfully. It reported Node `v24.14.1`, npm `11.11.0`, Claude Code CLI `2.1.140 (Claude Code)`, and MCP registration `codex_cc_reviewer is configured (codex-cc-reviewer@next)`.
- `doctor` warnings were non-blocking and already understood: Codex CLI was not runnable from PATH, and Claude Code had 4 blocked background jobs (`70dc29ab`, `92136ca5`, `ae94759f`, `ffa9a6c1`).

## Codex Restart Smoke

After restarting Codex, the restarted session called `cc_review` from the MCP tool loaded via `codex-cc-reviewer@next`.

The smoke review used temporary local evidence that was removed after the smoke:

- tracked diff in `docs/troubleshooting.md` with marker `codex-cc-reviewer-local-rc-smoke-0.3.1-rc.0`
- selected untracked text file `codex-smoke-config-secret.txt` with marker `codex-cc-reviewer-local-untracked-smoke-0.3.1-rc.0`

The smoke review returned:

- `ok: true`
- `task: review_diff`
- `model: opus`
- `elapsedMs: 87824`
- non-empty review
- `eventCount: 87`
- `costUsd: 0.29333975`
- cache details: `effective: hit`, `creationInputTokens: 18205`, `readInputTokens: 22717`
- captured `activityTail` and `transcriptTail`
- no MCP transport errors

The review cited concrete repository and packet evidence:

- `docs/troubleshooting.md`
- tracked marker `codex-cc-reviewer-local-rc-smoke-0.3.1-rc.0`
- `codex-smoke-config-secret.txt`
- untracked marker `codex-cc-reviewer-local-untracked-smoke-0.3.1-rc.0`
- untracked file byte count `112`
- tracked routing reason `risk: workflow_docs; source diff within budget`
- untracked routing reason `untracked_text; embedded with redactSecrets=true`
- redaction evidence `SMOKE_CONFIG_TOKEN=[REDACTED]`

Claude Code concluded: `SMOKE PASSED. All five acceptance criteria are satisfied based on packet evidence.`

## Cleanup

The temporary smoke artifacts were removed after the review.

Cleanup verification:

```powershell
git status --short
rg -n "codex-cc-reviewer-local-.*smoke|codex-smoke-config-secret|SMOKE_CONFIG_TOKEN|ROUTING_MARKER" .
```

`git status --short` was clean after cleanup, and the marker search returned no matches.
