# Local Codex Release Validation: v1.0.0

RC: v1.0.0-rc.0
Doctor: pass
Local-Codex-Smoke: pass
cc_review: pass

## Environment

- Date: 2026-05-16
- Maintainer: yiyuiii
- Repository: `Yiyuiii/codex-cc-reviewer`
- Branch: `next`
- RC commit: `a241fe4 chore: prepare v1.0.0-rc.0`
- RC tag: `v1.0.0-rc.0`
- npm dist-tag validated: `next -> 1.0.0-rc.0`
- GitHub prerelease: `https://github.com/Yiyuiii/codex-cc-reviewer/releases/tag/v1.0.0-rc.0`

## Commands

```powershell
npm run preflight
npm view codex-cc-reviewer@1.0.0-rc.0 version
npm view codex-cc-reviewer@next version
npm view codex-cc-reviewer dist-tags --json
npx --prefer-online -y codex-cc-reviewer@next install --package-spec codex-cc-reviewer@next
npx --prefer-online -y codex-cc-reviewer@next doctor
```

After restarting Codex, the restarted session invoked the MCP `cc_review` tool with:

```text
task: review_plan
model: opus
effort: max
permissionMode: bypassPermissions
tools: default
cwd: E:\Codes\codex-cc-reviewer
includeGitStatus: true
autoDiscoverGit: true
context: Post-restart local Codex smoke test for the published release candidate codex-cc-reviewer@1.0.0-rc.0.
```

## Results

- `npm run preflight`: passed before rc publication. It ran `npm ci`, typecheck, full Vitest suite, build, `npm pack --dry-run --json`, and CLI `--version` / `--help`.
- Vitest before rc publication: 19 files, 156 tests passed.
- Build: passed.
- Package dry run: produced `codex-cc-reviewer-1.0.0-rc.0.tgz` with 25 package entries.
- CLI version before rc publication: `1.0.0-rc.0`.
- GitHub Release workflow for `v1.0.0-rc.0`: passed publish and GitHub Release jobs. The publish job used npm Trusted Publishing provenance and `--tag next`.
- npm registry: `codex-cc-reviewer@1.0.0-rc.0` resolved to `1.0.0-rc.0`, `codex-cc-reviewer@next` resolved to `1.0.0-rc.0`, and dist-tags showed `next -> 1.0.0-rc.0`, `latest -> 0.3.1`.
- `doctor`: exited successfully. It reported Node `v24.14.1`, npm `11.11.0`, Claude Code CLI `2.1.140 (Claude Code)`, Codex config at `C:\Users\Administrator\.codex\config.toml`, and MCP registration `codex_cc_reviewer is configured (codex-cc-reviewer@next)`.
- `doctor` warnings were non-blocking and already understood: Codex CLI was not runnable from PATH, and Claude Code had 4 blocked background jobs (`70dc29ab`, `92136ca5`, `ae94759f`, `ffa9a6c1`).

## Codex Restart Smoke

After restarting Codex, the restarted session called `cc_review` from the MCP tool loaded via `codex-cc-reviewer@next`.

The smoke review returned:

- `ok: true`
- `task: review_plan`
- `model: opus`
- `elapsedMs: 282844`
- non-empty review
- `eventCount: 454`
- `costUsd: 1.0633165`
- cache details: `effective: hit`, `inputTokens: 13`, `creationInputTokens: 67298`, `readInputTokens: 299178`, `cacheCreation.ephemeral1hInputTokens: 67298`, `cacheCreation.ephemeral5mInputTokens: 0`
- captured `activityTail` and `transcriptTail`
- no MCP transport errors

The review cited concrete repository and release-gate evidence:

- `src/mcp/tools.ts:19-70`
- `src/mcp/server.ts:6-16`
- `.github/workflows/release.yml:53-64`
- `.github/workflows/release.yml:66-76`
- `.github/workflows/release.yml:78-109`
- `tests/release-assurance.test.ts:18-40`
- `AGENTS.md` release gate expectations
- `package.json:3`
- `package-lock.json`
- `src/index.ts:17`
- `src/mcp/server.ts:9`
- `CHANGELOG.md`
- `CHANGELOG.zh-CN.md`
- npm dist-tags: `next -> 1.0.0-rc.0`, `latest -> 0.3.1`

Claude Code concluded: `SMOKE PASS`, and confirmed stable promotion remained correctly gated on `.release-validation/v1.0.0.md`, stable version surfaces, and a `v1.0.0` tag reachable from `origin/main`.

The smoke used `review_plan` rather than `review_diff` because the `1.0.0-rc.0` headline does not change default diff routing or packet evidence behavior. The release content is the initial 1.x stability declaration, cache diagnostics display, maintainer research harness, documentation clarifications, and release assurance tests.

The promoted 1.x delta also includes an `availableTools`-conditioned routing guidance variant. The `tools: default` path remains byte-identical to the previously validated guidance; the post-restart smoke used `autoDiscoverGit: true`, so it exercised tracked diff routing through that default path. The custom-tools routing branch is covered by `tests/context-router.test.ts` and `tests/untracked-router.test.ts`.
