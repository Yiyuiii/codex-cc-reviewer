# Local Codex Release Validation: v0.2.3

RC: v0.2.3-rc.0
Doctor: pass
Local-Codex-Smoke: pass
cc_review: pass

## Environment

- Date: 2026-05-14
- Repository: `Yiyuiii/codex-cc-reviewer`
- Branch: `next`
- RC commit: `ccb658a chore: prepare 0.2.3-rc.0`
- RC tag: `v0.2.3-rc.0`
- npm dist-tag validated: `next -> 0.2.3-rc.0`

## Commands

```powershell
npm run preflight
npm view codex-cc-reviewer dist-tags --json
npm view codex-cc-reviewer@next version
npx --prefer-online -y codex-cc-reviewer@next --version
npx --prefer-online -y codex-cc-reviewer@next install --package-spec codex-cc-reviewer@next
npx --prefer-online -y codex-cc-reviewer@next doctor
```

## Results

- `npm run preflight`: passed. It ran `npm ci`, `npm run typecheck`, full Vitest suite, build, `npm pack --dry-run --json`, and CLI `--version` / `--help`.
- Vitest: 15 files, 78 tests passed.
- Build: passed.
- Package dry run: produced `codex-cc-reviewer-0.2.3-rc.0.tgz` manifest.
- CLI version: `0.2.3-rc.0`.
- npm dist-tags: `next` was `0.2.3-rc.0`; `latest` remained `0.2.2`.
- GitHub Release workflow for `v0.2.3-rc.0`: passed publish and GitHub Release jobs.
- `doctor`: exited successfully. It reported Node `v24.14.1`, npm `11.11.0`, Claude Code CLI `2.1.92`, and MCP registration `codex_cc_reviewer is configured (codex-cc-reviewer@next)`. It warned that Codex CLI was not runnable from PATH; Codex app MCP config still worked for the restarted smoke test.

## Codex Restart Smoke

After restarting Codex, the restarted session called `cc_review` from the MCP tool loaded via `codex-cc-reviewer@next`.

The smoke review returned:

- `ok: true`
- `task: review_doc`
- `model: opus`
- non-empty review
- captured activity, transcript, cache, cost, and event count details
- no MCP transport errors

The review cited repository evidence, including:

- `package.json`
- `package-lock.json`
- `src/index.ts`
- `src/mcp/server.ts`
- `CHANGELOG.md`
- `AGENTS.md`
- `.github/workflows/ci.yml`
- `.github/workflows/release.yml`
- `tests/release-assurance.test.ts`
- `.gitattributes`
- `.release-validation/`
- `git tag --list 'v0.2.3*'`
- `git log --oneline -5`

Claude Code reported no release-blocking issues for `v0.2.3-rc.0`. It noted two non-blocking future cleanups: the prerelease/stable changelog split and hardcoded version strings.
