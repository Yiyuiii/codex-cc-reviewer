# Local Codex Release Validation: v0.3.0

RC: v0.3.0-rc.0
Doctor: pass
Local-Codex-Smoke: pass
cc_review: pass

## Environment

- Date: 2026-05-14
- Maintainer: yiyuiii
- Repository: `Yiyuiii/codex-cc-reviewer`
- Branch: `next`
- RC commit: `132879d chore: prepare 0.3.0 rc`
- RC tag: `v0.3.0-rc.0`
- npm dist-tag validated: `next -> 0.3.0-rc.0`

## Commands

```powershell
npm run verify:release
npm view codex-cc-reviewer@0.3.0-rc.0 version
npm view codex-cc-reviewer@next version
npx --prefer-online -y codex-cc-reviewer@next install --package-spec codex-cc-reviewer@next
npx --prefer-online -y codex-cc-reviewer@next doctor
```

After restarting Codex, the restarted session invoked the MCP `cc_review` tool with:

```text
task: review_doc
model: opus
effort: medium
permissionMode: plan
tools: Read,Grep,LS
cwd: E:\Codes\codex-cc-reviewer
context: Post-restart local Codex smoke test for codex-cc-reviewer@next v0.3.0-rc.0. Please verify the tool is functional by inspecting this repository and producing a non-empty review that cites concrete repository evidence.
```

## Results

- `npm run verify:release`: passed. It ran typecheck, full Vitest suite, build, `npm pack --dry-run --json`, and CLI `--version` / `--help`.
- Vitest after the final RC patch: 17 files, 109 tests passed.
- Build: passed.
- Package dry run: produced `codex-cc-reviewer-0.3.0-rc.0.tgz` with 23 package entries.
- CLI version before stable bump: `0.3.0-rc.0`.
- GitHub Release workflow for `v0.3.0-rc.0`: passed publish and GitHub Release jobs. The publish job used npm Trusted Publishing provenance and `--tag next`.
- npm registry: `codex-cc-reviewer@0.3.0-rc.0` resolved to `0.3.0-rc.0`, and `codex-cc-reviewer@next` resolved to `0.3.0-rc.0`.
- `doctor`: exited successfully. It reported Node `v24.14.1`, npm `11.11.0`, Claude Code CLI `2.1.140 (Claude Code)`, and MCP registration `codex_cc_reviewer is configured (codex-cc-reviewer@next)`.
- `doctor` warnings were non-blocking: Codex CLI was not runnable from PATH, and Claude Code had 4 blocked background jobs (`70dc29ab`, `92136ca5`, `ae94759f`, `ffa9a6c1`).

## Codex Restart Smoke

After restarting Codex, the restarted session called `cc_review` from the MCP tool loaded via `codex-cc-reviewer@next`.

The smoke review returned:

- `ok: true`
- `task: review_doc`
- `model: opus`
- `elapsedMs: 69731`
- non-empty review
- `eventCount: 206`
- `costUsd: 0.3633885`
- cache details: `effective: disabled`, `creationInputTokens: 37078`, `readInputTokens: 64782`
- diagnostics: `1-hour cache hint is disabled for this review request.`
- captured `activityTail` and `transcriptTail`
- no MCP transport errors

The review cited repository evidence, including:

- `package.json:3`
- `src/index.ts:17`
- `src/mcp/server.ts:9`
- `.github/workflows/release.yml:39-51`
- `.github/workflows/release.yml:66-76`
- `.github/workflows/release.yml:86-88`
- `.github/workflows/release.yml:119-148`
- `.github/workflows/release.yml:171-189`
- `.github/workflows/release.yml:94-109`
- `AGENTS.md:47-85`

Claude Code reported no release-blocking issues for `v0.3.0-rc.0`. It noted non-blocking future cleanup opportunities around version-drift tests, validation-file ergonomics, release workflow comments, and documenting doctor warnings during stable promotion.
